public class Leet349{
    
    public int bin_search(in
    
    
    /**
     * Given two arrays,return their intersection 
     * Each element in the result must be unique ,and can be in any order 
     */
    public int[] intersection(int[] nums1,int[] nums2){
        //sort two arrays
        Arrays.sort(nums1);
        Arrays.sort(nums2);
        
        for(int i=0;i<length(nums1);i++){
            bin_search(nums2,0,length(nums2)-1);
            
        }//for
    }//intersection 
   
}//class